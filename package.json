{
    "name": "expense-splitter-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd backend && npm install && npx prisma@5.20.0 generate && cd ../frontend && npm install && npm run build"
    }
}